# ⛓️ Governance Flow & LockedGold
How Celo GovAI Hub interacts with Celo's on-chain governance. Details the flow: User locks CELO -> Delegates to Bot's Proxy -> Bot verifies via `_get_lockedgold_delegate_sync` -> User executes `/vote` via Telegram.
